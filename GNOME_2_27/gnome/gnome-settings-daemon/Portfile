# $Id$

PortSystem 1.0

name            gnome-settings-daemon
version         2.27.5
set branch      [join [lrange [split ${version} .] 0 1] .]
description     Gnome 2 settings daemon.
long_description ${description}
maintainers     devans openmaintainer
categories      gnome
platforms       darwin
homepage        http://www.gnome.org/
master_sites    gnome:sources/${name}/${branch}/

checksums       md5     bdb36927b3060b66b88fe5a0f132e8ae \
                sha1    637aa03f2cf02cce183313c9e5ae38c6731dda2b \
                rmd160  9cb056824bc4cf8387417f630fdbfe327000479e

depends_lib     port:gnome-desktop \
                port:libnotify \
                port:libgnomekbd \
                port:libglade2 \
                port:gst-plugins-good

depends_build	port:pkgconfig \
                port:intltool \
                port:p5-xml-parser \
                port:gnome-doc-utils

use_bzip2 	yes

pre-configure {
    reinplace "s|-bundle|-bundle -undefined dynamic_lookup|g" ${worksrcpath}/configure
}

configure.args  --disable-schemas-install \
                --disable-pulse

post-activate    {
    system "export GCONF_CONFIG_SOURCE=`gconftool-2 --get-default-source` && \
        gconftool-2 --makefile-install-rule ${prefix}/etc/gconf/schemas/*.schemas"
}

livecheck.check regex
livecheck.url   http://ftp.gnome.org/pub/GNOME/sources/${name}/${branch}/
livecheck.regex {LATEST-IS-([0-9\.]+)}
